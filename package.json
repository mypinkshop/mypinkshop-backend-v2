{
  "name": "mypinkshop-backend-hono",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.js",
    "deploy": "wrangler deploy --minify src/index.js",
    "build": "esbuild src/index.js --bundle --outfile=dist/index.js --platform=neutral --format=esm"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0",
    "esbuild": "^0.19.0"
  }
}
